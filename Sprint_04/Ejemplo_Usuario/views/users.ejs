<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title> Listado de Usuarios</title>
</head>
<body>
    <h1>Listado de Usuario</h1>
    <ul>
        <% users.forEach(user => {%>
            <li>
                <a href="/users/<%= user.id%>"> <%= user.nombre%> (Edad: <%=user.edad %></a>
            </li>
        <% }); %>
    </ul>
</body>
</html>